class Car : Piece
{
    // public bool isValidMove(Location _location)
    // {
    //     return (isColumnSame(_location) ^ isRowSame(_location));
    // }


    public Car(Colour _colour) 
    {
        colour = _colour;
    }
}